{
    "bsonType": "object",
    "required": ["login", "password", "e-mail"],
    "properties": {
        "login": {
            "bsonType": "string",
            "description":  "must be a string and is required"
        },
        "password": {
            "bsonType": "string",
            "description":  "must be a string and is required"
        },
        "e-mail": {
            "bsonType": "string",
            "description":  "must be a string and is required"
        }
    }
}