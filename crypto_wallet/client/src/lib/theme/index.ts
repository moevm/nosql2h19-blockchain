import colors from './colors'
import breakpoints from './breakpoints'
import common from './common'
import sizes from './sizes'
export { default as muiDefaultTheme } from './mui'

export const defaultTheme = {
  colors,
  breakpoints,
  common,
  sizes
}
