const styles = (theme: App.Theme) => ({
  wrapper: {
    display: 'grid',
    gridTemplateColumns: '90% 10%',
    alignItems: 'center',
    width: '100%'
  },
  title: {
    textAlign: 'center'
  },
  statistics: {
    gridArea: 'statistics'
  }
})

export default styles
