const styles = (theme: App.Theme) => ({
  input: {
    width: '100%',
    height: 40,
    padding: 20,
    font: 'inherit',
    backgroundColor: theme.colors.components.input.bg,
    border: 'none',
    borderRadius: 5
  }
})

export default styles
