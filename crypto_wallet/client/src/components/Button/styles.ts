const styles = (theme: App.Theme) => ({
  button: {
    height: 40,
    padding: '0 20px',
    font: 'inherit',
    color: '#fff',
    backgroundColor: '#212121',
    border: 'none',
    borderRadius: 5,
    cursor: 'pointer',
    '&:hover': {
      backgroundColor: '#555'
    }
  }
})

export default styles
