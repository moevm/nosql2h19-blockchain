const styles = (theme: App.Theme) => ({
  container: {
    position: 'fixed',
    top: 0,
    right: 0,
    bottom: 0,
    left: 0,
    zIndex: -1,
    visibility: 'hidden'
  }
})

export default styles
